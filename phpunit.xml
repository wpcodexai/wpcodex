<?xml version="1.0" encoding="UTF-8"?>
<phpunit
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
    bootstrap="tests/bootstrap.php"
    colors="true"
    stopOnFailure="false"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <!--
            Integration tests require a running WordPress install.
            The directory is declared here but PHPUnit will skip it gracefully
            if it does not exist yet (phpunit.xml is version-controlled;
            the directory is created when integration tests are added).
        -->
        <testsuite name="Integration">
            <directory suffix="Test.php">tests/Integration</directory>
        </testsuite>
    </testsuites>

    <!--
        Code coverage source: includes/ is the PSR-4 root for all plugin PHP.
        src/ contains only TypeScript and SCSS — not PHP, not measured here.
    -->
    <source>
        <include>
            <directory suffix=".php">includes</directory>
        </include>
        <exclude>
            <!-- Exclude the entry point — it is bootstrap-only, not testable logic. -->
            <file>allyworker.php</file>
        </exclude>
    </source>
</phpunit>